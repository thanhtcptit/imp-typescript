func gcd(x, y) {
    while y != 0
        if y > x
            tmp := x;
            x := y;
            y := tmp
        else
            x := x - y
        end
    end;
    return x
};

func main() {
    r := gcd(128, 72);
    return 0
}
